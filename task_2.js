let a = Number(prompt('Enter the value of a ')); // instead of Number we can use parseint for type conversion
let b = Number(prompt('Enter the value of b '));
const result = a + b
console.log("Sum of two numbers:- ", result)
document.write("Sum of two numbers:- ", result)
